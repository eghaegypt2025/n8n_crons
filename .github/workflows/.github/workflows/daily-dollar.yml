name: Daily Dollar Webhook

on:
  schedule:
    - cron: "0 7 * * *"
  workflow_dispatch:

jobs:
  call_webhook:
    runs-on: ubuntu-latest
    steps:
      - name: Call n8n webhook
        run: |
          curl -sS -X GET "https://n8n-latest-viqu.onrender.com/webhook/daily-dollar"
